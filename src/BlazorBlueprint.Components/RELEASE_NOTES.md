## What's New in v2.4.2

### Bug Fixes
- DateRangePicker day name headers now correctly rotate based on the `FirstDayOfWeek` parameter
- Disabled dates are no longer visually highlighted within a selected date range
- Selected day count now excludes disabled dates from the total
- Presets that resolve entirely to disabled dates are now ignored
